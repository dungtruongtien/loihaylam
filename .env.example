# Node environment
NODE_ENV=production

# Server port (Railway sets this automatically — do not set manually on Railway)
# PORT=9999

# SQLite database path
# On Railway: mount a Volume at /app/data and set this to /app/data/game.db
DATABASE_PATH=/app/data/game.db
